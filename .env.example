# Copy this file to .env and fill in real values.
# NEVER commit the real .env — it is gitignored.
#
# Add API keys and secrets here as tools require them, e.g.:
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# FIRECRAWL_API_KEY=
